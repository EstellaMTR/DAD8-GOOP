public class HelloWorld {
    // the main file of this class
    // we need to inform the compiler of this
    // we use methods for this. Specifically, main. In this case, maina
    // args are the arguments we pass to the program in the terminal
    // the return type used here is void
    // the main method MUST be defined like this. It cannot have a different return type.
    // static methods are methods that can be run without having any kind of object in memory.
    // we have no object before we actually run this
    /*
        Multiline comment
     */
    static void main(String[] args) {
        // Go to the system. Go to the standard output of the system. Print this string. println means print newline
        System.out.println("Hello!");
    }
}
